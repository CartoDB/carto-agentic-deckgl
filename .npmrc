# Disable npm's default git commit and tag creation on `npm version`.
# The postversion script handles the git ceremony with a custom commit
# message format: chore(release): vX.Y.Z
git-tag-version=false
