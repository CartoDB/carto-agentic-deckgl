name: Tests

on:
  workflow_call:

jobs:
  # ── Core library + backends (run in parallel with frontends) ──
  backends:
    name: Core & Backends
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # Build map-ai-tools (dependency for all backends)
      - name: Install map-ai-tools dependencies
        working-directory: ./map-ai-tools
        run: npm ci

      - name: Build map-ai-tools
        working-directory: ./map-ai-tools
        run: npm run build

      - name: Test map-ai-tools
        working-directory: ./map-ai-tools
        run: npm test

      # Vercel AI SDK
      - name: Install Vercel AI SDK dependencies
        working-directory: ./backend-integration/vercel-ai-sdk
        run: npm ci

      - name: Test Vercel AI SDK
        working-directory: ./backend-integration/vercel-ai-sdk
        run: npm test

      # OpenAI Agents SDK
      - name: Install OpenAI Agents SDK dependencies
        working-directory: ./backend-integration/openai-agents-sdk
        run: npm ci

      - name: Test OpenAI Agents SDK
        working-directory: ./backend-integration/openai-agents-sdk
        run: npm test

      # Google ADK
      - name: Install Google ADK dependencies
        working-directory: ./backend-integration/google-adk
        run: npm install --force

      - name: Test Google ADK
        working-directory: ./backend-integration/google-adk
        run: npm test

  # ── Frontends (run in parallel with backends) ──
  frontends:
    name: Frontends
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      # Build map-ai-tools (dependency for all frontends)
      - name: Install map-ai-tools dependencies
        working-directory: ./map-ai-tools
        run: npm ci

      - name: Build map-ai-tools
        working-directory: ./map-ai-tools
        run: npm run build

      # Angular
      - name: Install Angular dependencies
        working-directory: ./frontend-integration/angular
        run: pnpm install --frozen-lockfile

      - name: Test Angular
        working-directory: ./frontend-integration/angular
        run: pnpm test

      # React
      - name: Install React dependencies
        working-directory: ./frontend-integration/react
        run: pnpm install --frozen-lockfile

      - name: Test React
        working-directory: ./frontend-integration/react
        run: pnpm test

      # Vanilla
      - name: Install Vanilla dependencies
        working-directory: ./frontend-integration/vanilla
        run: pnpm install --frozen-lockfile

      - name: Test Vanilla
        working-directory: ./frontend-integration/vanilla
        run: pnpm test

      # Vue
      - name: Install Vue dependencies
        working-directory: ./frontend-integration/vue
        run: pnpm install --frozen-lockfile

      - name: Test Vue
        working-directory: ./frontend-integration/vue
        run: pnpm test
